# ==========================================
# Ultra-Lightweight Requirements (512MB RAM)
# ==========================================

# Core Flask
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Ultra-light AI - Only sentence-transformers
sentence-transformers==2.5.0

# Data processing (minimal versions)
pandas==2.1.4
numpy==1.24.0

# PyTorch CPU-only (CRITICAL for 512MB RAM)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu

# Database
mysql-connector-python==8.2.0

# HTTP
requests==2.31.0

# Environment
python-dotenv==1.0.1

# ==========================================
# IMPORTANT NOTES:
# ==========================================
# 1. scikit-learn REMOVED - not needed for FAQ matching
# 2. Using CPU-only PyTorch to save ~300MB RAM
# 3. Model: all-MiniLM-L6-v2 (80MB, best for 512MB)
# 4. No transformers library - only sentence-transformers
# 5. Total estimated memory: ~400MB peak usage
